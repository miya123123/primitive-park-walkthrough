# 三人称プレイヤー スモークテスト

実施日: 2026-08-26
対象: Primitive Park Walkthrough

## 自動検証

```text
/usr/local/bin/godot --headless --path . --script tests/run_all_tests.gd --log-file /private/tmp/primitive_park_third_person_final.log
ALL TESTS PASSED (155 assertions)
```

`.godot`を除外した一時コピーでも同じランナーを実行し、`ALL TESTS PASSED (155 assertions)`を確認した。通常・キャッシュなしの最終ログに`ERROR`、`SCRIPT ERROR`、`WARNING`、`Parse Error`、`Invalid`、`leaked`はない。

## 実画面検証

- 中央背面追従の来園者モデルと、プリミティブだけの園内を同時に確認。
- マウスドラッグで三人称カメラを旋回し、モデルを画面内に保持。
- 施設へ接近してSpringArm3Dによる近接カメラ表示を確認。
- 既存の操作説明は`MOUSE ORBIT`へ更新され、中央クロスヘアは表示されない。
- 実画面の確認後、デバッグゲームを停止。

詳細は[`evidence/third-person-park-playtest.md`](evidence/third-person-park-playtest.md)を参照。

## 補足

Godotエディターの再インポートは終了コード0で完了した。実行環境ではユーザー領域の`editor_settings-4.6.tres`保存が許可されずエディター設定保存エラーが出たが、プロジェクトファイル、アセット再インポート、テスト結果には影響しない。
